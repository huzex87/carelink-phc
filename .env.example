# CareLink PHC: Production Environment Schema
# Duplicate this file to .env and fill in the secrets

# --- Database ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=carelink_phc
DB_USER=postgres
DB_PASS=REPLACE_WITH_SECURE_PASSWORD

# --- Security ---
# Critical: Replace with a 64-character random string
JWT_SECRET=REPLACE_WITH_SECURE_64_CHAR_RANDOM_STRING

# --- Interoperability (DHIS2) ---
DHIS2_URL=https://play.dhis2.org/demo
DHIS2_USER=admin
DHIS2_PASS=REPLACE_WITH_DHIS2_PASSWORD

# --- SMTP (Alert Notifications) ---
SMTP_HOST=smtp.your-institutional-mail.gov
SMTP_PORT=587
SMTP_USER=alerts@your-domain.gov
SMTP_PASS=REPLACE_WITH_SMTP_PASSWORD
